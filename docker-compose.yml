version: '3.8'

services:
  generator-app:
    build: .
    container_name: file-generator-service
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - ./generated:/app/generated
      - ./logs:/app/logs
    environment:
      - TZ=Europe/Moscow
    restart: unless-stopped